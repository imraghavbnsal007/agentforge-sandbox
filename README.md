# Sample Repo

A tiny Python project that AgentForge's agent operates on. Each agent run
copies this directory into a scratch workspace, edits the copy, and runs
pytest there — this directory itself is never modified.
